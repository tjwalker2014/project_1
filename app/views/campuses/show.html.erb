<p>
  <strong>Name:</strong>
  <%= @campus.name %>
</p>

<p>
  <strong>Courses running at this campus:</strong>
  <% @campus.courses.each do |course| %>

    <p><%= course.name %></p>
    <p><%= course.room.name %></p>

    <% end %>
</p>

<p>
  <strong>Students at this campus:</strong>
  <% @campus.users.each do |user| %>

    <p><%= user.name %></p>

    <% end %>
</p>



<p>
  <%= link_to "Edit", edit_campus_path(@campus) %> |
  <%= link_to "Destroy", @campus, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", campuses_path %>
</p>
